const mongoose = require('mongoose'); require('dotenv').config(); mongoose.connect(process.env.MONGO_URI || 'mongodb://127.0.0.1:27017/ev_rental_dev'); require('./models/vehicleModel'); const Booking = require('./models/bookingModel'); async function check() { const vehicleId = '6a5798466d50ff3feae9decb'; const bookings = await Booking.find({ vehicle: vehicleId }).sort({ createdAt: -1 }); console.log('Total bookings for vehicle:', bookings.length); bookings.forEach(b => { console.log('ID:', b._id, 'Status:', b.booking_status, 'Dates:', b.start_date, 'to', b.end_date); }); process.exit(0); } check();
