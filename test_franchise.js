const mongoose = require('mongoose'); require('dotenv').config(); mongoose.connect(process.env.MONGO_URI || 'mongodb://127.0.0.1:27017/ev_rental_dev'); const FranchiseStore = require('./models/franchiseStoreModel'); async function check() { const fId = '6a5798456d50ff3feae9deca'; const f = await FranchiseStore.findById(fId); console.log('Franchise exists?', !!f); if(f) { console.log('Payment model:', f.payment_model); } process.exit(0); } check();
