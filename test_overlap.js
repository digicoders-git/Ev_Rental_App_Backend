const mongoose = require('mongoose'); require('dotenv').config({path: '../../.env'}); mongoose.connect(process.env.MONGO_URI || 'mongodb://127.0.0.1:27017/ev_rental_dev'); const Booking = require('./models/bookingModel'); async function test() { const pending = await Booking.find({ booking_status: 'pending' }).sort({ createdAt: -1 }).limit(1).populate('vehicle'); if(!pending.length) { console.log('No pending booking found'); return; } const b = pending[0]; console.log('Pending Booking:', b._id, 'Vehicle:', b.vehicle._id, 'Dates:', b.start_date, 'to', b.end_date); const overlaps = await Booking.find({ _id: { $ne: b._id }, vehicle: b.vehicle._id, booking_status: { $in: ['confirmed', 'ongoing'] }, start_date: { $lte: b.end_date }, end_date: { $gte: b.start_date } }); console.log('Overlaps found:', overlaps.length); overlaps.forEach(o => { console.log('Overlap ID:', o._id, 'Status:', o.booking_status, 'Dates:', o.start_date, 'to', o.end_date); }); process.exit(0); } test();
